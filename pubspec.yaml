name: fluuter
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4


dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^18.0.1
  permission_handler: ^11.3.1
  #flutter_native_splash: ^2.2.16  # flutter pub run flutter_native_splash:create


  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.1
  fluttertoast: ^8.2.10

 # url_launcher: ^6.0.10 # Aggiungi questa riga


dev_dependencies:
  flutter_launcher_icons: ^0.14.2

#flutter_native_splash:
#  image: "assets/images/ic.png"  # Percorso dell'immagine
#  color: "#0000FF"  # Colore di sfondo (può essere un codice esadecimale)
#  android: true
#  ios: true
#  web: false  # Aggiungi se vuoi usare anche per il Web

flutter_icons:
  android: true
  ios: true
  macos: true
  windows: true
  image_path: "assets/icon/small_ic.png"  # Percorso dell'immagine dell'icona

  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/images/info_picture.png
    - assets/social_network_icon/facebook.png
    - assets/social_network_icon/ig.png
    - assets/social_network_icon/snapchat.png

  fonts:
    - family: YsabeauSC
      fonts:
        - asset: fonts/YsabeauSC-Regular.ttf


